[project]
name = "ctn-stack"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Anuruth Lertpiya", email = "l2k.bobby@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
